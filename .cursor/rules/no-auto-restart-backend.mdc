---
description: 不要自动启动或重启后端；仅用户手动管理后端进程
alwaysApply: true
---

# 后端进程由用户手动管理

- **禁止**在改完 Go/后端代码后自动 `go run`、重启、杀端口再拉起后端。
- **禁止**为「验证改动生效」主动重启 `backend` / `:9180` 服务。
- 需要新代码生效时：**提醒用户自行手动重启**即可，不要代为执行启动命令。
- 仅当用户在当前对话中**明确要求**启动/重启/关闭后端时，才可操作相关进程。
- 前端 `npm run dev` 等同理：不要擅自重启；除非用户明确要求。
